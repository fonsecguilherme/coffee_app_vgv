name: coffee_app_vgv
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  ## State Management  
  bloc: ^9.1.0
  flutter_bloc: ^9.1.1
  bloc_test: ^10.0.0
  equatable: ^2.0.7

  ## Network
  http: ^1.5.0

  ## Utils  
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
